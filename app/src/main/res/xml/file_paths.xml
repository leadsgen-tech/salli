<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Matches `context.getExternalFilesDir(null)` where TransactionExporter writes CSVs. -->
    <external-files-path name="exports" path="." />
</paths>
