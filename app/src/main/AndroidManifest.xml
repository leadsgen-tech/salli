<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Core SMS permissions: receive real-time, read historical. -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- Deliberately NO network permission of any kind. The app cannot open a socket, so SMS
         content cannot leave the device. WorkManager's library manifest merges in
         ACCESS_NETWORK_STATE (for network-constrained work) and FOREGROUND_SERVICE (for
         expedited/foreground workers); Salli uses neither, so strip both from the final APK. -->
    <uses-permission
        android:name="android.permission.ACCESS_NETWORK_STATE"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE"
        tools:node="remove" />

    <application
        android:name=".SalliApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="${salliAppLabel}"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Salli"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Salli">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".sms.SmsReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter android:priority="999">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <!-- Handles the inline "who was this for?" reply from a transaction prompt
             notification. Not exported — only our own PendingIntent can fire it. -->
        <receiver
            android:name=".sms.TransactionReplyReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="lk.salli.app.TX_REPLY" />
            </intent-filter>
        </receiver>

        <!-- Home-screen widget (Glance). Exported so the launcher can bind it. -->
        <receiver
            android:name=".widget.SalliWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/salli_widget_info" />
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Hilt injects the HiltWorkerFactory into SalliApplication during onCreate. If we
             leave WorkManager's default startup initializer enabled, it runs BEFORE Hilt has
             injected the factory and falls back to the default factory — which then can't
             instantiate our @AssistedInject-annotated workers ("NoSuchMethodException: …
             SmsIngestWorker.<init>…"). Removing the default initializer makes WorkManager
             wait for Configuration.Provider on our Application. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>

</manifest>
